// Place your key bindings in this file to overwrite the defaults
[
	{
	   "key": "ctrl+d",
	   "command": "workbench.action.tasks.runTask",
	   "args": "PyFRC: Deploy current project to the RoboRio and run it"
	},
	{
		"key": "ctrl+shift+d",
		"command": "workbench.action.tasks.runTask",
		"args": "PyFRC: Debug robot.py with simulator"
	 }
]